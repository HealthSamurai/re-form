language: clojure
lein: lein2

script:
- cd example && lein with-profile prod cljsbuild once  && ls build

after_success:
- 'git config --global user.email "robot@health-samurai.io"'
- 'git config --global user.name "Travis CI Deployer"'
# - 'if [ -n "$TRAVIS_TAG" ]; then ./script/release.sh; fi'
